import 'package:flutter/material.dart';
class SubServiceView extends StatelessWidget {
  const SubServiceView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
